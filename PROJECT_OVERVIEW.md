# العَب — توثيق المشروع الشامل
> آخر تحديث: 14 أبريل 2026

---

## 📌 نظرة عامة

**العَب** هو تطبيق موبايل لمجتمع الرياضة، يتيح للمستخدمين تنظيم مباريات رياضية، الانضمام إليها، تكوين مجموعات رياضية، وتقييم اللاعبين. يدعم التطبيق حالياً ثلاثة رياضات: **كرة القدم، البادل، التنس**.

### المكدس التقني (Tech Stack)
| المكوّن | التقنية |
|---|---|
| تطبيق الموبايل | Expo / React Native (expo-router) |
| الخادم الخلفي | Node.js + Express |
| قاعدة البيانات | PostgreSQL + Drizzle ORM |
| تحسين الأداء | Indexed DB (PostgreSQL Indexes), Bulk Queries (N+1 Query Resolution), React.memo() |
| إدارة الحالة | React Query (TanStack) |
| التصميم | Visual Silence (Minimalist Luxury) / Liquid Glass 2026 |
| الاتجاه | تدعم اللغتين (عربي/إنجليزي) - RTL/LTR |
| تدويل (i18n) | i18next + expo-localization |
| Push Notifications | Expo Push Notifications |
| رفع الملفات | Replit Object Storage |

### هيكل المستودع (Monorepo - pnpm)
```
/
├── artifacts/
│   ├── mobile/          ← تطبيق الموبايل (Expo)
│   ├── api-server/      ← الخادم الخلفي (Express)
│   └── mockup-sandbox/  ← بيئة تصميم (Canvas Mockups)
├── lib/
│   ├── db/              ← Schema قاعدة البيانات (Drizzle ORM)
│   ├── api-spec/        ← مواصفة OpenAPI (العقد بين Frontend وBackend)
│   ├── api-zod/         ← مخططات Zod المولّدة تلقائياً
│   └── api-client-react/← React Query Hooks المولّدة تلقائياً
```

---

<!-- AUTO-UPDATED: Do not edit the section marked DYNAMIC below manually -->

## 📊 إحصائيات المشروع (DYNAMIC-START)
- **آخر تحديث:** 20 أبريل 2026
- **آخر مهمة مدموجة:** 33
- **عدد المهام المنتهية:** 33
- **عدد صفحات التطبيق:** 23
- **عدد مسارات الـ API:** 66
- **عدد جداول قاعدة البيانات:** 16
<!-- DYNAMIC-END -->

---

## 📱 صفحات التطبيق

### أولاً: تدفق التسجيل والمصادقة (Auth Flow)

| الصفحة | المسار | الوصف |
|---|---|---|
| الترحيب | `/` | شاشة البداية — اختيار الرياضة (كرة قدم / بادل / تنس) |
| إدخال الهاتف | `/phone` | إدخال رقم الهاتف لطلب OTP |
| التحقق من الرمز | `/otp` | إدخال الرمز المرسل عبر SMS |
| إنشاء الملف الشخصي | `/profile-setup` | الاسم، الصورة، تفضيلات اللعب (مستخدمون جدد فقط) |
| اختيار المركز | `/position-selector` | اختيار مراكز اللعب المفضلة لكل رياضة |

**تدفق التنقل:**
```
index → phone → otp → (مستخدم جديد) → profile-setup → position-selector → (tabs)
                    → (مستخدم موجود) → (tabs)
```

---

### ثانياً: التبويبات الرئيسية (Main Tabs)

| التبويب | الأيقونة | الوصف |
|---|---|---|
| الرئيسية | 🏠 | عرض المباريات القادمة والنشاط الأخير |
| الاستكشاف | 🔍 | اكتشاف المباريات العامة (قائمة + خريطة) |
| مبارياتي | ⚽ | المباريات التي انضممت إليها أو نظّمتها |
| المجموعات | 👥 | المجموعات الرياضية التي أنا عضو فيها |
| الملف الشخصي | 👤 | بياناتي، إحصائياتي، مؤشر الموثوقية |

---

### ثالثاً: شاشات المباريات (Match Screens)

| الصفحة | المسار | من يصلها | إلى أين تؤدي |
|---|---|---|---|
| تفاصيل المباراة | `/match-details` | من الرئيسية / الاستكشاف / مبارياتي / إشعار | إدارة المباراة (للمنظم) / التقييم (بعد الانتهاء) |
| إنشاء مباراة | `/create-match` | زر + في الرئيسية أو صفحة المجموعة | العودة للرئيسية بعد الإنشاء |
| تعديل المباراة | `/edit-match` | من إدارة المباراة | العودة لإدارة المباراة |
| إدارة المباراة | `/manage-match` | تفاصيل المباراة (للمنظم فقط) | تعديل المباراة / تقييم اللاعبين |
| تقييم ما بعد المباراة | `/post-match-rating` | من تفاصيل المباراة (بعد انتهائها) | الرئيسية |

**صلاحيات الوصول:**
- **المنظم فقط**: يرى زر "إدارة المباراة"، يمكنه التعديل والإلغاء وتسجيل الحضور
- **اللاعبون**: يمكنهم الانضمام/المغادرة وتقييم الآخرين بعد انتهاء المباراة
- **تقييم المباراة الملغاة**: مغلق (لا يُسمح بالتقييم)
- **منع التقييم المزدوج**: لا يمكن تقييم نفس اللاعب مرتين في نفس المباراة

---

### رابعاً: شاشات المجموعات (Group Screens)

| الصفحة | المسار | الوصف |
|---|---|---|
| تفاصيل المجموعة | `/group-detail` | معلومات المجموعة، الأعضاء، مباريات المجموعة |
| إدارة المجموعة | `/group-management` | لوحة تحكم المدير: الأعضاء، طلبات الانضمام، الأدوار |
| محادثة المجموعة | `/group-chat` | دردشة نصية لأعضاء المجموعة (polling-based) |
| إنشاء مجموعة | `/create-group` | نموذج إنشاء مجموعة جديدة |

**تدفق التنقل:**
```
المجموعات (tab) → group-detail → group-chat
                              → group-management (للمدير فقط)
                              → match-details (مباريات المجموعة)
                              → create-match (إنشاء مباراة للمجموعة)
```

---

### خامساً: الشاشات المساعدة (Utility Screens)

| الصفحة | المسار | الوصف |
|---|---|---|
| الإشعارات | `/notifications` | سجل الإشعارات — ضغط الإشعار يوجه للمباراة/المجموعة |
| الإعدادات | `/settings` | إدارة الحساب، تفضيلات الإشعارات، تسجيل الخروج |
| رابط الدعوة | `/invite/[token]` | معالجة روابط الدعوة العميقة (Deep Links) |

---

## 🔗 خريطة الترابط الكاملة

```
[AUTH FLOW]
index ──► phone ──► otp ──► profile-setup ──► position-selector ──► (TABS)
                        └──────────────────────────────────────────► (TABS)

[TABS]
Home ──────────────────────────────────────────────────────────────────────────┐
Explore ──────────────────────────────────────────────────────────────────────►│ match-details
My Matches ───────────────────────────────────────────────────────────────────►│   ├─► manage-match ──► edit-match
Notifications ────────────────────────────────────────────────────────────────►│   └─► post-match-rating
Invite Link ──────────────────────────────────────────────────────────────────►│
                                                                               │
Groups ──────────────────────► group-detail ──────────────────────────────────►│ match-details
                          │        ├──► group-chat                             │
                          │        ├──► group-management                       │
                          │        └──► create-match                           │
                          └──► create-group                                    │
                                                                               │
Profile ──────────────────────► settings ──► [LOGOUT] ──► index               │
                 └──────────────────────────────────────────────────────────────┘
```

---

## 🚀 مميزات التطبيق الكاملة

### 1. نظام المصادقة والأدوار
- تسجيل دخول عبر رقم الهاتف + OTP (بدون كلمة مرور)
- JWT tokens لإدارة الجلسات و Push Token يُلغى تلقائياً عند تسجيل الخروج
- **لوحة تحكم للمسؤولين (Admin Dashboard)**: إدارة الملاعب والمستخدمين مركزياً.

### 2. إدارة المباريات
- **إنشاء مباراة**: العنوان، الرياضة، التاريخ والوقت، الملعب (من قائمة معتمدة)، الحد الأقصى للاعبين.
- **قائمة الانتظار (Waitlist)**: الانضمام تلقائياً لقائمة الانتظار عند امتلاء المباراة وإشعار عند توفر شاغر.
- **الانضمام / المغادرة**: مع كشف التعارض الزمني (لا تنضم لمباراتين في نفس الوقت).
- **البحث والفلترة**: حسب الرياضة، التاريخ، المستوى، الموقع.
- **دفتر القطة (Gatta)**: تتبع تكاليف المباراة وحالة الدفع والتذكيرات.
- **تسجيل الحضور والإكمال التلقائي**: المنظم يسجل החضور، أو يكمل النظام المباراة تلقائياً.
- **روابط الدعوة**: إنشاء رابط خاص لدعوة لاعبين لمباراة.

### 3. نظام المجموعات
- إنشاء مجموعات رياضية (عامة/خاصة)
- إدارة الأعضاء والأدوار (مالك، مدير، عضو)
- طلبات الانضمام للمجموعات الخاصة (قبول/رفض)
- دعوة مستخدمين مباشرة للمجموعة
- إنشاء مباريات مرتبطة بالمجموعة
- دردشة نصية جماعية (polling كل 5 ثوانٍ)
- روابط دعوة للمجموعات

### 4. نظام الملاعب والتقييم
- **قاعدة الملاعب المعتمدة**: ملاعب موثقة لكل رياضة (كرة قدم، بادل، تنس).
- **اقتراح ملاعب**: يمكن للمستخدمين اقتراح ملاعب جديدة، يراجعها الأدمن.
- **تقييم الملاعب**: تقييم من 1 إلى 5 نجوم بعد كل مباراة.
- **تقييم اللاعبين (الشارات)**: تقييم زملاء اللعب بـ (🎨 Artist, 🪨 Rock, ⚡ Bolt).
- متصل بالمباريات المكتملة فقط، مع تصويت لدقة مستوى اللاعب.

### 5. مؤشر الموثوقية (Reliability Index™)
نظام تلقائي لحساب موثوقية اللاعب (0–100):

```
شروط التفعيل: انضمام لـ 3 مباريات على الأقل (كلاعب، ليس منظماً)

الحساب:
  الأساس  = (المباريات التي حضرتها / إجمالي مبارياتك) × 100
  المكافأة = min(10, مجموع تقييماتك × 0.5)
  النتيجة  = min(100, الأساس + المكافأة)

قبل 3 مباريات: يظهر "—" (غير محدد)
```

### 6. تحسينات الأداء الفائقة (Performance Optimizations)
تم تجهيز التطبيق ليتحمل الضغط العالي (Production Ready):
- **الخادم (Backend)**: استعلامات مجمعة (Bulk API Queries) لحل مشكلة N+1، مما يقلل الاستهلاك بصورة هائلة.
- **قاعدة البيانات (Database)**: فهارس (Indexes) لكافة الحقول الحيوية لتسريع البحث والجلب.
- **الواجهة (Frontend)**: تفعيل التخزين المؤقت عبر `TanStack Query` وتخفيض الـ Re-renders باستخدام `React.memo` وتحسين قوائم الـ `FlatList`.

### 6. الإشعارات والتذكيرات الذكية
- **تذكيرات ذكية**: تذكير قبل المباراة بساعة، تذكير تقييم الملاعب واللاعبين بعد المباراة.
- **تذكير النشاط الأسبوعي**: للمستخدمين غير النشطين (اختياري).
- **الإشعارات التلقائية**: انضمام لاعب، إلغاء مباراة، رسائل مجموعات، وتقييمات.
- التحكم الكامل في الإعدادات وصلاحية POST_NOTIFICATIONS (Android 13+).

### 7. الملف الشخصي
- الصورة الشخصية (رفع للـ Object Storage)
- الاسم والرياضات المفضلة
- اختيار مراكز اللعب (متعددة لكل رياضة)
- مستويات اللعب (البادل: P1–P8، التنس: 1.0–7.0)
- مؤشر الموثوقية وإجمالي الشارات
- إحصائيات المباريات

---

## 🛠️ API — نقاط النهاية

جميع المسارات تعمل تحت البادئة `/api`.

### المصادقة (2 مسار)
```
POST /auth/request-otp   ← طلب OTP لرقم هاتف
POST /auth/verify-otp    ← التحقق والحصول على JWT
```

### المباريات و قائمة الانتظار (15 مسار)
```
GET    /matches                              ← قائمة المباريات
GET    /matches/{id}                         ← تفاصيل مباراة
POST   /matches                              ← إنشاء مباراة جديدة
DELETE /matches/{id}                         ← إلغاء مباراة
POST   /matches/{id}/join                    ← الانضمام لمباراة (تنقلك لقائمة الانتظار إن كانت ممتلئة)
POST   /matches/{id}/leave                   ← مغادرة مباراة
PUT    /matches/{id}/attendance              ← تسجيل حضور
PATCH  /matches/{id}                         ← تعديل بيانات
PATCH  /matches/{id}/players/{userId}/p...   ← تعديل دفع لاعب
DELETE /matches/{id}/players/{playerId}      ← إزالة لاعب
POST   /matches/{id}/payment-reminder        ← تذكير المتأخرين بالدفع
POST   /matches/{id}/mark-attendees-paid     ← دفع جماعي
POST   /matches/{id}/invite-link             ← إنشاء رابط
GET    /matches/{id}/level-votes/status      ← حالة تصويت المستخدم
POST   /matches/{id}/level-votes             ← تقديم التقييم
```

### المجموعات (18 مسار)
```
GET    /groups                                         ← قائمة المجموعات العامة والمنضم إليها
GET    /groups/{id}                                    ← تفاصيل مجموعة والأعضاء
POST   /groups                                         ← إنشاء مجموعة جديدة
PUT    /groups/{id}                                    ← تعديل معلومات المجموعة
DELETE /groups/{id}                                    ← حذف المجموعة (المالك فقط)
POST   /groups/{id}/join                               ← طلب الانضمام لمجموعة
POST   /groups/{id}/leave                              ← مغادرة مجموعة
GET    /groups/{id}/join-requests                      ← قائمة طلبات الانضمام (المدير فقط)
POST   /groups/{id}/join-requests/{requestId}/approve  ← قبول طلب انضمام
POST   /groups/{id}/join-requests/{requestId}/reject   ← رفض طلب انضمام
POST   /groups/{id}/invite                             ← دعوة مستخدم مباشرة للمجموعة
POST   /groups/{id}/invite-link                        ← إنشاء رابط دعوة للمجموعة
GET    /invites/{token}                                ← التحقق من رابط دعوة وجلب بيانات الهدف
POST   /invites/{token}/accept                         ← قبول الدعوة والانضمام
DELETE /groups/{id}/members/{userId}                   ← إزالة عضو من المجموعة
PATCH  /groups/{id}/members/{userId}/role              ← تغيير دور العضو
GET    /groups/{id}/messages                           ← جلب رسائل الدردشة
POST   /groups/{id}/messages                           ← إرسال رسالة في الدردشة
```

### المستخدمون (6 مسارات)
```
GET    /users/me                          ← ملفي الشخصي ومؤشر الموثوقية
PATCH  /users/me                          ← تعديل ملفي الشخصي
GET    /users/me/notification-settings    ← إعدادات الإشعارات
PATCH  /users/me/notification-settings    ← تعديل إعدادات الإشعارات
POST   /users/me/avatar                   ← رفع صورة شخصية
GET    /users/{id}                        ← الملف العام لمستخدم آخر
```

### الإشعارات (5 مسارات) + Push (2 مسار)
```
GET    /notifications                ← سجل الإشعارات
PATCH  /notifications/read-all       ← تحديد جميع الإشعارات كمقروءة
PATCH  /notifications/{id}/read      ← تحديد إشعار معين كمقروء
DELETE /notifications/{id}           ← حذف إشعار معين
DELETE /notifications                ← حذف جميع الإشعارات
POST   /push/register                ← تسجيل Expo Push Token
DELETE /push/unregister              ← إلغاء تسجيل Push Token (عند تسجيل الخروج)
```

### الملاعب و الإدارة (12 مسار إضافي)
```
GET    /venues                               ← الملاعب المعتمدة
GET    /venues/{id}                          ← تفاصيل ملعب وتقييماته
POST   /venues/suggest                       ← اقتراح ملعب جديد
POST   /venues/{id}/reviews                  ← تقييم ملعب بعد المباراة
GET    /venues/{id}/reviews                  ← جلب تقييمات ملعب
GET    /admin                                ← واجهة الداشبورد (HTML)
GET    /admin/dashboard                      ← إحصائيات الأدمن
GET    /admin/venues/suggestions             ← الاقتراحات المعلقة
POST   /admin/venues/suggestions/{id}/approve← اعتماد اقتراح
POST   /admin/venues                         ← إضافة ملعب مباشرة
GET    /admin/users                          ← إدارة المستخدمين وتعيين أدمن
POST   /admin/seed-admin                     ← توليد حساب الأدمن الأول
```

### متنوع (6 مسارات)
```
GET  /healthz            ← فحص صحة الخادم
GET  /invite/{token}     ← هبوط رابط الدعوة
GET  /match/{id}         ← ملخص مباراة
GET  /group/{id}         ← ملخص مجموعة
GET  /profile/{userId}   ← ملخص مستخدم
GET  /storage/avatar     ← بروكسي للصور
```

---

## 🗄️ قاعدة البيانات

### الجداول الرئيسية

| الجدول | الأعمدة الرئيسية | الوصف |
|---|---|---|
| `users` | id, phone, name, avatar_url, sports, skill_level, sport_profiles, reliability, notif_match, notif_group, notif_rating | بيانات المستخدمين وإعدادات الإشعارات |
| `otp_codes` | id, phone, code, expires_at, used | رموز التحقق عبر SMS |
| `matches` | id, title, sport, date, time, venue, lat, lng, max_players, cost, is_public, organizer_id, invited_group_id, session_type, skill_level, status | المباريات والتدريبات |
| `match_players` | id, match_id, user_id, position, attended, attendance_status, paid | اللاعبون في كل مباراة |
| `groups` | id, name, sport, description, is_public, admin_id | المجموعات الرياضية |
| `group_members` | id, group_id, user_id, role | أعضاء المجموعات والأدوار |
| `group_messages` | id, group_id, sender_id, sender_name, text | رسائل دردشة المجموعات |
| `group_join_requests` | id, group_id, user_id, status, requested_at, reviewed_at, reviewed_by | طلبات الانضمام للمجموعات |
| `ratings` | id, match_id, rater_id, rated_user_id, score, rating_type, level_accuracy_vote | تقييمات اللاعبين |
| `notifications` | id, user_id, type, title, body, related_id, read | الإشعارات داخل التطبيق |
| `push_tokens` | id, user_id, token | رموز Push Notifications |
| `invite_links` | id, token, target_type, target_id | روابط الدعوة للمباريات والمجموعات |
| `venues` | id, name, sport, address, rating, status | الملاعب المعتمدة ببياناتها الجغرافية |
| `venue_suggestions` | id, name, sport, suggested_by, status | اقتراحات المستخدمين للملاعب الجديدة |
| `venue_reviews` | id, venue_id, match_id, user_id, rating | تقييم كل مستخدم للملعب بعد المباراة |
| `match_waitlist` | id, match_id, user_id | اللاعبون المصطفون لدخول مباراة ممتلئة |
| `reminder_logs` | id, match_id, user_id, type | سجل التذكيرات المرسلة لمنع التكرار |

---

## 📋 سجل التطوير (History)

| # | المهمة | الحالة |
|---|---|---|
| 1 | اختبار دورة حياة المباراة كاملة | ✅ |
| 2 | دمج بوابة الدفع (Gatta - دفتر القطة) | ✅ |
| 3 | واجهة محادثة المجموعات | ✅ |
| 4 | إتمام دورة حياة المباراة تلقائياً | ✅ |
| 5 | تحسين تجربة المستخدم والصقل النهائي | ✅ |
| 6 | إعادة تصميم واجهة اختيار المراكز | ✅ |
| 7 | ملعب بادل تفاعلي + تخطي بدون مركز | ✅ |
| 8 | دعم اختيار أكثر من مركز في الملف الشخصي | ✅ |
| 9 | نظام اختيار المستوى الاحترافي (بادل P1-P8 / تنس 1.0-7.0) | ✅ |
| 10 | إعادة تصميم الإعدادات (Liquid Glass 2026) | ✅ |
| 11 | إعادة تصميم الإشعارات (Liquid Glass 2026) | ✅ |
| 12 | إعادة تصميم شاشة تعديل المباراة (Liquid Glass 2026) | ✅ |
| 13 | توحيد تصميم صفحة تفاصيل المجموعة | ✅ |
| 14 | اختبار شامل: المصادقة + المباريات | ✅ |
| 15 | اختبار شامل: المجموعات + الملف الشخصي + التقييم | ✅ |
| 16 | إصلاح منطق زر التقييم (ملغاة + منع التكرار) | ✅ |
| 17 | إصلاح روابط المشاركة والدعوة | ✅ |
| 18 | إصلاح إلغاء Push Token عند تسجيل الخروج | ✅ |
| 19 | Android: StatusBar شفاف + Edge-to-Edge | ✅ |
| 20 | Android: تحسين FlatList لـ 60fps | ✅ |
| 21 | Android: صلاحية POST_NOTIFICATIONS لـ Android 13+ | ✅ |
| 22 | Android: إعداد EAS Build | ✅ |
| 23 | إدارة المجموعات المتقدمة (أدوار + طلبات انضمام) | ✅ |
| 24 | روابط الدعوة للمجموعات والمباريات | ✅ |
| 25 | اختبار التطبيق على محاكاة iOS و Android | ✅ |
| 26 | تحديث أوصاف مستويات البادل والتنس الرسمية | ✅ |
| 27 | تحسين دقة سؤال المستوى في شاشة التقييم | ✅ |
| 28 | تطبيق نظام الهوية البصرية "Visual Silence" (فخامة وبساطة) | ✅ |
| 29 | دعم اللغات المتعددة (i18n) - واجهات عربية وإنجليزية بالكامل | ✅ |
| 30 | دمج وتوحيد رموز الترجمة وحل تعارضات TypeScript | ✅ |
| 31 | نظام إدارة وتقييم الملاعب + لوحة تحكم الإدارة (Admin Dashboard) | ✅ |
| 32 | قائمة الانتظار للمباريات الممتلئة (Waitlist) | ✅ |
| 33 | نظام التذكير الذكي (قبل/بعد المباراة والتذكير الأسبوعي) | ✅ |

---

## 🎨 الهوية البصرية

- **الأسلوب**: Visual Silence + Glassmorphism + Liquid Glass 2026
- **الفلسفة**: تقليل الضجيج البصري، إزالة الحدود (Borders)، الاعتماد على التباين اللوني (Tonal Layering).
- **الاتجاه**: دعم كامل للـ RTL و LTR (عربي/إنجليزي)
- **الخلفية**: تدرجات داكنة مع طبقات زجاجية شفافة
- **الألوان الخاصة بكل رياضة**: كرة قدم (أخضر)، بادل (أزرق)، تنس (برتقالي)
- **الخطوط**: خط عربي واضح مع دعم كامل للـ RTL
- **الحواف**: مدوّرة (border-radius كبير)
- **التأثيرات**: BlurView, LinearGradient, Shadow على كل البطاقات
