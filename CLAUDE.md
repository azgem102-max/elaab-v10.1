# العَب — دليل العمل مع الذكاء الاصطناعي
> مستوحى من [gstack](https://github.com/garrytan/gstack) بتكييف كامل لمشروع العَب

---

## 📌 معلومات المشروع الجوهرية

- **الاسم**: العَب (elaab)
- **التقنية**: Expo/React Native + Node.js/Express + PostgreSQL/Drizzle
- **الهيكل**: Monorepo (pnpm)
  - `artifacts/mobile/` — تطبيق الموبايل
  - `artifacts/api-server/` — الخادم الخلفي
  - `lib/db/` — Schema قاعدة البيانات
  - `lib/api-spec/` — عقد OpenAPI
- **الهوية البصرية**: Visual Silence + Liquid Glass 2026
- **اللغات**: عربي (RTL) + إنجليزي (LTR) — i18next
- **الرياضات**: كرة قدم 🟢، بادل 🔵، تنس 🟠

---

## 🎮 أوامر workflow (مشابه لـ gstack)

اكتب الأمر في رسالتك وسأنفذه وفق الـ checklist الخاصة به.

---

### `/think` — التفكير أولاً قبل البناء
```
اريد [وصف الميزة]
/think
```
**ماذا يفعل:**
- يسألك عن المشكلة الحقيقية وليس الحل المقترح
- يحدد الشاشات والـ API endpoints المتأثرة
- يقترح 3 مناهج مختلفة للتنفيذ مع مقارنة المزايا والعيوب
- يوصي بأضيق مسار للشحن السريع
- يكتب خطة تنفيذ مفصلة قبل أي كود

---

### `/plan` — مراجعة الخطة الهندسية
```
/plan [وصف الميزة أو الملف داخل implementation_plan.md]
```
**checklist المراجعة:**
- [ ] هل الخطة تؤثر على Schema قاعدة البيانات؟ (migration)
- [ ] هل تحتاج endpoints جديدة في الـ API؟
- [ ] هل تؤثر على ملفات i18n (en.ts / ar.ts)؟
- [ ] هل تؤثر على RTL/LTR؟
- [ ] هل المتغيرات موجودة في .env؟
- [ ] هل تحتاج مكونات Glass جديدة؟
- [ ] مخطط تدفق البيانات (Mobile → API → DB)
- [ ] حالات الخطأ والـ edge cases

---

### `/design` — مراجعة التصميم (Visual Silence)
```
/design [اسم الشاشة أو المكوّن]
```
**checklist التصميم:**
- [ ] يستخدم مكونات Glass (GlassCard, GlassButton, GlassScreenHeader)
- [ ] لا حدود صلبة (borders) — استخدم Tonal Layering
- [ ] الألوان مناسبة للرياضة (⚽ أخضر / 🏓 أزرق / 🎾 برتقالي)
- [ ] BlurView + LinearGradient على البطاقات
- [ ] الخطوط: Tajawal/Alexandria للعربي، Inter للإنجليزي
- [ ] RTL يعكس التصميم بشكل صحيح
- [ ] لا نصوص مباشرة — كل نص من ملفات i18n
- [ ] Micro-animations على التفاعلات
- [ ] Dark mode فقط (لا light mode)
- [ ] border-radius كبير على كل العناصر

---

### `/review` — مراجعة الكود قبل الدمج
```
/review [اسم الملف أو المجلد]
```
**checklist الكود:**
- [ ] لا `any` types في TypeScript
- [ ] كل نصوص العرض من i18n (لا hardcoded strings عربية/إنجليزية)
- [ ] API calls تستخدم React Query hooks وليس fetch مباشر
- [ ] Error handling مكتوب (try/catch أو onError في React Query)
- [ ] Loading states موجودة لكل async operation
- [ ] لا console.log متروكة في الكود النهائي
- [ ] JWT token موجود في كل request محمي
- [ ] لا secrets في الكود (استخدم .env)
- [ ] permissions منفذة للمنظم vs اللاعب
- [ ] مخرجات API تتطابق مع OpenAPI spec

---

### `/qa` — اختبار وظيفي شامل
```
/qa [اسم الميزة أو الـ flow]
```
**سيناريوهات الاختبار الأساسية:**

**Auth Flow:**
- [ ] إدخال رقم هاتف صحيح → OTP يُرسل
- [ ] رمز OTP خاطئ → رسالة خطأ واضحة
- [ ] مستخدم جديد → profile-setup → position-selector
- [ ] مستخدم موجود → مباشرة للـ tabs

**إنشاء وإدارة المباريات:**
- [ ] إنشاء مباراة بجميع الحقول المطلوبة (الملعب من القائمة المعتمدة)
- [ ] المنظم لا يمكنه الانضمام كلاعب
- [ ] الانضمام لمباراة ممتلئة → يضع اللاعب في قائمة الانتظار للتبديل (Waitlist)
- [ ] كشف التعارض الزمني (مباراتان في نفس الوقت)
- [ ] إرسال التذكيرات (قبل الإضافية وبعد المباراة) بشكل صحيح دون تكرار

**نظام المجموعات:**
- [ ] إنشاء مجموعة خاصة → طلبات انضمام تعمل
- [ ] المدير يقبل/يرفض طلبات الانضمام
- [ ] الدردشة تتحدث كل 5 ثوانٍ (polling)

**التقييم (Ratings):**
- [ ] التقييم مفتوح فقط بعد انتهاء المباراة
- [ ] لا يمكن تقييم نفسك
- [ ] لا يمكن تقييم نفس اللاعب مرتين
- [ ] مباراة ملغاة → التقييم مغلق

**الإشعارات:**
- [ ] انضمام لاعب → إشعار للمنظم
- [ ] إلغاء مباراة → إشعار لجميع اللاعبين
- [ ] تسجيل الخروج → يلغي Push Token

---

### `/security` — مراجعة الأمان
```
/security [اسم الـ route أو الملف]
```
**checklist الأمان:**
- [ ] JWT يُتحقق منه في كل route محمي (middleware)
- [ ] التحقق من صلاحية المنظم قبل الحذف/التعديل
- [ ] لا SQL injection (Drizzle ORM يحمي لكن تحقق من raw queries)
- [ ] OTP codes تنتهي صلاحيتها ولا تُعاد استخدامها
- [ ] Push Tokens مرتبطة بالمستخدم الصحيح
- [ ] Object Storage URLs غير قابلة للتخمين
- [ ] Rate limiting على نقاط OTP
- [ ] إزالة لاعب: التحقق من أن الطالب هو المنظم فقط
- [ ] Invite links تنتهي صلاحيتها (expires_at)

---

### `/ship` — الشحن والنشر
```
/ship [وصف التغييرات]
```
**checklist قبل الشحن:**
- [ ] `pnpm build` يمر بدون أخطاء TypeScript
- [ ] جميع ملفات i18n متزامنة (en.ts و ar.ts)
- [ ] migration قاعدة البيانات موجودة ومختبرة
- [ ] .env.example محدّث بالمتغيرات الجديدة
- [ ] PROJECT_OVERVIEW.md محدّث بالإحصائيات الجديدة
- [ ] لا debug logs في الكود
- [ ] الـ API endpoints الجديدة موثقة في api-spec

---

### `/retro` — ملخص ما تم إنجازه
```
/retro
```
**يجمع:**
- قائمة المهام المكتملة منذ آخر retro
- الملفات المتغيرة وعددها
- الـ endpoints الجديدة
- الإصلاحات المهمة
- الاقتراحات للخطوة القادمة

---

### `/investigate` — تحقيق في مشكلة
```
/investigate [وصف المشكلة أو رسالة الخطأ]
```
**يفعل:**
- يحلل رسالة الخطأ ويتتبع مصدرها
- يفحص Mobile → API → DB وأين يحدث الخطأ
- يقترح 3 احتمالات للسبب مع الحل
- يفحص ملفات الـ logs والـ console

---

### `/i18n` — مراجعة الترجمة
```
/i18n [اسم الملف أو الشاشة]
```
**يتحقق من:**
- [ ] كل مفاتيح ar.ts مقابلها في en.ts والعكس
- [ ] لا نصوص عربية/إنجليزية مباشرة في الكود
- [ ] RTL layout يعمل مع جميع المفاتيح
- [ ] نصوص الأخطاء مترجمة
- [ ] التواريخ والأرقام مُنسّقة حسب اللغة

---

## 🏗️ قواعد البناء (لا تكسرها أبداً)

### قاعدة 1: الكود الجديد لا يكسر الموجود
كل تغيير يجب أن يشمل:
- اختبار الشاشات المتأثرة قبل وبعد
- التحقق من أن الـ API لا يزال يستجيب بنفس الشكل

### قاعدة 2: Database migrations لا تُحذف
كل تغيير في Schema يحتاج migration جديد — لا تعديل في الـ migrations القديمة.

### قاعدة 3: i18n دائماً أولاً
أي نص يُعرض للمستخدم يجب أن يكون في ملفات الترجمة قبل وضعه في الكود.

### قاعدة 4: Glass Components فقط
لا تنشئ مكوناً جديداً بدون التحقق أولاً من وجوده في:
`artifacts/mobile/components/glass/`

### قاعدة 5: OpenAPI Contract أولاً
أي endpoint جديد يُوثَّق أولاً في `lib/api-spec/` ثم يُنفَّذ في الـ backend ثم الـ frontend.

---

## 📁 سيناريوهات مراجعة الملفات

### إذا عدّلت شاشة موبايل:
```
artifacts/mobile/app/[screen].tsx
→ تحقق من: i18n keys, Glass components, RTL, API hook
```

### إذا عدّلت API route:
```
artifacts/api-server/src/routes/[route].ts
→ تحقق من: JWT middleware, صلاحيات، Zod validation, error responses
```

### إذا عدّلت Schema:
```
lib/db/schema/[table].ts
→ تحقق من: migration file, TypeScript types, Drizzle relations
```

### إذا أضفت ترجمة:
```
artifacts/mobile/i18n/en.ts AND ar.ts
→ تحقق من: تطابق المفاتيح، نصوص RTL صحيحة
```

---

## 🔄 Sprint Workflow الموصى به

```
فكرة → /think → /plan → تنفيذ → /review → /qa → /security → /ship → /retro
```

**مدة Sprint مقترحة:** 1–3 sessions
**الشحن:** كل ميزة كاملة مستقلة (لا نصف ميزات)
